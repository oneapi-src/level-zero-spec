#
# Copyright (C) 2023 Intel Corporation
#
# SPDX-License-Identifier: MIT
#
# See YaML.md for syntax definition
#
--- #--------------------------------------------------------------------------
type: header
desc: "Intel $OneApi Level-Zero Tool Experimental Extension for Global Metric Timestamps"
version: "1.5"
--- #--------------------------------------------------------------------------
type: macro
desc: "Global Metric Timestamps Experimental Extension Name"
version: "1.5"
name: $T_GLOBAL_METRIS_TIMESTAMPS_EXP_NAME
value: '"$T_experimental_global_metric_timestamps"'
--- #--------------------------------------------------------------------------
type: enum
desc: "Global Metric Timestamps Experimental Extension Version(s)"
version: "1.5"
name: $t_global_metric_timestamps_exp_version_t
etors:
    - name: "1_0"
      value: "$X_MAKE_VERSION( 1, 0 )"
      desc: "version 1.0"
--- #--------------------------------------------------------------------------
type: struct
desc: "Metric timestamps resolution"
version: "1.5"
class: $tMetric
name: $x_metric_timestamps_resolution_exp_t
base: $x_base_desc_t
members:
    - type: uint64_t
      name: timerResolution
      desc: Returns the resolution of device timer in nanoseconds used for timestamps.
    - type: uint64_t
      name: timestampValidBits
      desc: "[out] Returns the number of valid bits in the timestamp value."
details:
    - "This structure may be returned from $tMetricGroupGetProperties via `pNext` member of $t_metric_group_properties_t"
    - "Used for determining the total number of EUs available on device."
--- #--------------------------------------------------------------------------
type: function
desc: "Returns metric timestamps synchronized with global device timestamps, optionally synchronized with host"
class: $tMetricGroup
name: GetTimestampsExp
decl: static
details:
    - "The application may call this function from simultaneous threads."
params:
    - type: "$t_metric_group_handle_t"
      name: hMetricGroup
      desc: "[in] handle of the metric group"
    - type: "$x_bool_t"
      name: synchronizedWithHost
      desc: "[in] Return host synchronized or device synchronized metric timestamp."
      init: "false"
    - type: "uint64_t"
      name: globalTimestamp
      desc: |
            [out] Timestamp synchronized to host or device.
    - type: "uint64_t"
      name: metricTimestamp
      desc: |
            [out] Metric timestamp.